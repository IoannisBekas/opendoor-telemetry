# --- HTTP / parsing -------------------------------------------------------
httpx>=0.27.0
lxml>=5.2.0               # required by pandas.read_html for the EDGAR R-files

# --- data -----------------------------------------------------------------
polars>=1.0.0             # streaming TSV ingestion (Redfin bulk files)
pandas>=2.2.0             # read_html only
pyarrow>=16.0.0           # polars <-> parquet staging

# --- storage --------------------------------------------------------------
psycopg[binary]>=3.1.0

# --- feeds ----------------------------------------------------------------
fredapi>=0.5.2

# --- config ---------------------------------------------------------------
PyYAML>=6.0
python-dotenv>=1.0.0
